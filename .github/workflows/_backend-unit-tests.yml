name: Backend Unit Tests (Reusable)

on:
  workflow_call:
    inputs:
      dotnet-version:
        type: string
        default: '10.0.x'
      configuration:
        type: string
        default: 'Release'
      build-artifact-name:
        type: string
        default: 'backend-build-output'

permissions:
  contents: read
  checks: write

jobs:
  unit-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    defaults:
      run:
        working-directory: backend

    env:
      UNIT_TEST_PROJECT: ./tests/HousingApp.Application.Tests/HousingApp.Application.Tests.csproj

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
          cache: true
          cache-dependency-path: |
            backend/**/packages.lock.json
            backend/Directory.Packages.props
            backend/**/*.csproj

      - name: Download compiled backend output
        uses: actions/download-artifact@v8
        with:
          name: ${{ inputs.build-artifact-name }}
          path: backend/

      - name: Run unit tests
        run: |
          dotnet test ${{ env.UNIT_TEST_PROJECT }} \
            --configuration ${{ inputs.configuration }} \
            --no-build --no-restore \
            --verbosity normal \
            --logger "trx;LogFileName=unit-tests.trx" \
            --results-directory ./TestResults

      - name: Publish unit test report
        id: unit-test-report
        uses: dorny/test-reporter@v3
        if: ${{ !cancelled() }}
        with:
          name: Backend Unit Tests
          path: backend/TestResults/unit-tests.trx
          reporter: dotnet-trx
          use-actions-summary: true
          report-title: Backend Unit Tests
          badge-title: unit tests
          fail-on-error: true
          fail-on-empty: true

      - name: Write unit test summary
        if: ${{ !cancelled() }}
        run: |
          {
            echo "## Backend Unit Tests"
            echo ""
            echo "| Metric | Value |"
            echo "| --- | --- |"
            echo "| Conclusion | ${{ steps.unit-test-report.outputs.conclusion }} |"
            echo "| Passed | ${{ steps.unit-test-report.outputs.passed }} |"
            echo "| Failed | ${{ steps.unit-test-report.outputs.failed }} |"
            echo "| Skipped | ${{ steps.unit-test-report.outputs.skipped }} |"
            echo "| Report | ${{ steps.unit-test-report.outputs.url_html }} |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: backend-unit-test-results
          path: backend/TestResults/
          retention-days: 7
