name: .NET Integration testing

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['develop']

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    uses: ./.github/workflows/backend-ci-reusable.yml
    with:
      run-unit-tests: true
      run-integration-tests: true
      run-publish: false

